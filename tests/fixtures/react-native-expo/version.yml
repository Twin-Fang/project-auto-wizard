# ===
# Version format: x.y.z (semantic versioning)
# - patch: auto-incremented on each main push (unless release PR)
# - version_code: monotonically increasing build number, auto-incremented parallel to version
# ===
# Supported project types: spring, flutter, next, react, react-native, react-native-expo, node, python, basic
# Multi-type projects: use project_types array (e.g., ["spring", "react"])
# Monorepo paths: use project_paths map to specify subdir per type (e.g., {"spring": "api", "react": "web"})
# ===

version: "0.0.1"
version_code: 1
project_type: "react-native-expo"
project_types:
  - "react-native-expo"
metadata:
  last_updated: "2026-07-08"
  update_reason: "initial scaffold"
